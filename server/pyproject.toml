[project]
name = "itable-server"
version = "0.1.0"
description = ""
requires-python = ">=3.13"
dependencies = [
    "aiomoex>=2.1.2",
    "async-lru>=2.0.4",
    "dacite>=1.9.2",
    "django>=5.1.6",
    "load-dotenv>=0.1.0",
    "psycopg[binary]>=3.2.4",
    "uvicorn>=0.34.0",
]

[dependency-groups]
dev = [
    "coverage>=7.6.12",
    "ruff>=0.9.6",
]
